/** @file
  Processor containers for the Apple T8140 family.

  Generated by Tools/add-soc.py from the device tree for
  Apple MacBook Neo.

  Covers: t8140

  UNVERIFIED. Every address, core count and window below is what the device
  tree states; none of it has been read off this silicon by us, and no machine
  in this family has been booted. Treat a successful build as evidence that the
  package is self-consistent and nothing more.

  A separate SSDT rather than part of a board DSDT, because the CPU tree is a
  property of the die and the DSDT is a property of the board. Keeping it here
  is what lets one board package -- J414sBoardPkg, GenericBoardPkg -- serve
  machines built on different silicon.

  6 core(s) in 2 cluster(s) across 1 die(s).

  Scope (\_SB) is opened by the DSDT; this only adds to it.

 * Copyright (c) 2026 Aurora Silicon
  SPDX-License-Identifier: BSD-2-Clause-Patent OR MIT
**/

DefinitionBlock ("Cpus.aml", "SSDT", 0x02, "Apple", "T8140CPU", 0x8140) {
    Scope (\_SB) {
    Device (DIE0) {
        Name (_HID, "ACPI0010")
        Name (_UID, 0)

        // 4 efficiency core(s), device-tree cluster 0.
        Device (CL00) {
            Name (_HID, "ACPI0010")
            Name (_UID, 1)

            Device (C000) {
                Name (_HID, "ACPI0007")
                Name (_UID, 0)
                Name (_STR, Unicode ("Apple A18 Pro Efficiency Core 0"))
                Name (_DDN, "Apple A18 Pro Efficiency Core 0")
                Method (_STA) { Return (0xF) }
            }

            Device (C001) {
                Name (_HID, "ACPI0007")
                Name (_UID, 1)
                Name (_STR, Unicode ("Apple A18 Pro Efficiency Core 1"))
                Name (_DDN, "Apple A18 Pro Efficiency Core 1")
                Method (_STA) { Return (0xF) }
            }

            Device (C002) {
                Name (_HID, "ACPI0007")
                Name (_UID, 2)
                Name (_STR, Unicode ("Apple A18 Pro Efficiency Core 2"))
                Name (_DDN, "Apple A18 Pro Efficiency Core 2")
                Method (_STA) { Return (0xF) }
            }

            Device (C003) {
                Name (_HID, "ACPI0007")
                Name (_UID, 3)
                Name (_STR, Unicode ("Apple A18 Pro Efficiency Core 3"))
                Name (_DDN, "Apple A18 Pro Efficiency Core 3")
                Method (_STA) { Return (0xF) }
            }
        }

        // 2 performance core(s), device-tree cluster 1.
        Device (CL01) {
            Name (_HID, "ACPI0010")
            Name (_UID, 2)

            Device (C004) {
                Name (_HID, "ACPI0007")
                Name (_UID, 4)
                Name (_STR, Unicode ("Apple A18 Pro Performance Core 0"))
                Name (_DDN, "Apple A18 Pro Performance Core 0")
                Method (_STA) { Return (0xF) }
            }

            Device (C005) {
                Name (_HID, "ACPI0007")
                Name (_UID, 5)
                Name (_STR, Unicode ("Apple A18 Pro Performance Core 1"))
                Name (_DDN, "Apple A18 Pro Performance Core 1")
                Method (_STA) { Return (0xF) }
            }
        }
    }
    }
}
